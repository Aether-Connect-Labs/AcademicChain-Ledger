{
  "name": "academicchain-ledger-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm install && npm run build --prefix client",
    "deploy": "npx wrangler pages deploy client/dist",
    "lint": "npm run lint --prefix client"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "wrangler": "^4.70.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
  }
}
