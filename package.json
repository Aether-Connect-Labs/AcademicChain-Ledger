{
  "name": "academicchain-ledger-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm install wrangler --no-save",
    "deploy": "echo 'WARNING: Do not run deploy manually in Cloudflare Pages. Use git push.' && exit 0",
    "lint": "npm run lint --prefix client"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "wrangler": "^4.70.0"
  },
  "devDependencies": {
  }
}
