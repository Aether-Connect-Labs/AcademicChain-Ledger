{
  "name": "academicchain-ledger-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "deploy": "npm run build --prefix client && npx wrangler pages deploy client/dist --project-name academicchain-ledger",
    "lint": "npm run lint --prefix client"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "wrangler": "^4.70.0"
  },
  "devDependencies": {
  }
}
